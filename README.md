# Liber — Шаблон Цифровой Библиотеки

**Liber** — это современный, адаптивный фронтенд-шаблон для веб-сайта цифровой библиотеки. Проект представляет собой лендинг с каталогом книг, расширенным поиском и презентацией контента, выполненный в чистом, типографическом стиле.

## 🛠 Используемые технологии

Проект написан на нативных веб-технологиях без использования CSS-фреймворков (как Bootstrap или Tailwind), что обеспечивает высокую производительность и легкость кода.

### HTML5
- **Семантическая верстка**: Использование тегов `<header>`, `<nav>`, `<main>`, `<article>`, `<section>`, `<footer>` для правильной структуры документа.
- **SVG Sprites**: Иконки реализованы через систему SVG Symbols (в начале `<body>`), что позволяет переиспользовать их через тег `<use>` и легко стилизовать через CSS.
- **Schema.org Microdata**: В код внедрена микроразметка для улучшения SEO.
    - `Schema.org/Book` — для карточек книг.
    - `Schema.org/Person` — для авторов.
    - `Schema.org/AggregateRating` — для рейтингов.
    - `Schema.org/SiteNavigationElement` — для навигации.

### CSS3
- **CSS Variables (Custom Properties)**: Цветовая палитра и шрифты вынесены в `:root` для легкой кастомизации темы.
- **Flexbox**: Используется для навигации, выравнивания элементов внутри карточек и футера.
- **CSS Grid**: Используется для построения адаптивных сеток каталога книг (`grid-template-columns`) и блоков преимуществ.
- **Mobile First**: Стили написаны сначала для мобильных устройств, а затем расширяются через медиа-запросы (`min-width: 640px`, `768px`, `1024px`).
- **Glassmorphism**: Использование `backdrop-filter: blur()` для создания эффекта матового стекла в навигации и элементах интерфейса.
- **Типографика**: Подключены шрифты Google Fonts (*Crimson Text*, *DM Sans*, *Roboto Mono*).

### JavaScript
- Подключен файл `js/script.js` (логика для мобильного меню и кнопки "Наверх" должна быть реализована в этом файле).

## ✨ Функционал и Особенности

1.  **Адаптивность**: Интерфейс плавно перестраивается от мобильного вида (1 колонка) до широких экранов (4 колонки).
2.  **Интерактивные карточки**: Эффекты наведения (hover), тени и масштабирование изображений.
3.  **Умный поиск (UI)**: Стилизованное поле поиска в главном блоке.
4.  **Каталог книг**: Секция с карточками, включающими обложку, кнопку "Избранное", жанр, рейтинг (звезды) и кнопку "Читать".
5.  **Доступность (A11y)**: Использование `aria-label` для кнопок без текста.

## 📂 Структура проекта

```text
/
├── index.html          # Главная страница (разметка)
├── css/
│   └── style.css       # Таблица стилей
├── js/
│   └── script.js       # Скрипты (меню, скролл)
└── images/             # Изображения (заглушки)
    ├── hero-library.jpg
    ├── book-great-gatsby.jpg
    ├── book-dune.jpg
    └── ...
